# Restaurante React 

Proyecto de reserva de mesas deazrrollado# Sistema de Reserva de Restaurantes

## Descripción

Este proyecto es una aplicación web desarrollada con **React** y **Vite** que permite gestionar el flujo de reservas de un restaurante mediante una interfaz moderna e intuitiva.

## Requisitos

Antes de ejecutar el proyecto, asegúrese de tener instalado:

* Node.js (versión 18 o superior recomendada)
* npm (incluido con Node.js)

## Instalación

1. Clonar el repositorio:

```bash
git clone <URL_DEL_REPOSITORIO>
```

2. Ingresar a la carpeta del proyecto:

```bash
cd <NOMBRE_DEL_PROYECTO>
```

3. Instalar las dependencias:

```bash
npm install
```

## Ejecución

Para iniciar el servidor de desarrollo, ejecutar:

```bash
npm run dev
```

La aplicación estará disponible en la dirección que indique la terminal, normalmente:

```text
http://localhost:5173
```

## Compilación para producción

Si se desea generar la versión de producción:

```bash
npm run build
```

Para previsualizar la versión compilada:

```bash
npm run preview
```

## Tecnologías utilizadas

* React
* Vite
* JavaScript
* HTML5
* CSS3

## Integrantes

* Alejandro Colqe Vega
* Jeremy Sullca Pino
* Franklin Quispe Malca
* Wily Huamanquispe
* Joel Robles Estrada
* Rodrigo Palomino

## Notas

* Si el proyecto se descarga por primera vez, es necesario ejecutar `npm install` antes de utilizar `npm run dev`.
* La carpeta `node_modules` no se incluye en el repositorio, ya que se genera automáticamente al instalar las dependencias.
 en React
